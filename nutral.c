#include<stdio.h>
//write a program to check givin noumber nutral or non nutral.
	main()
	{
		int n;
		
		printf("Enter Noumber to check nutral or non nutrol");
		scanf("%d",&n);
		
		if(n == 0)
		{
			printf("\nNoumber is nutral..");
		}
		else
		{
			printf("\nNoumber is non nutral");
		}
		
	}
